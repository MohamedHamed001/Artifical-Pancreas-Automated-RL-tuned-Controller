# Runtime requirements for ap-rl (Artificial Pancreas RL-tuned controller).
# Pinned as ranges compatible with TF 2.10–2.15 on macOS / Linux.

numpy>=1.21
scipy>=1.7
pandas>=1.3
matplotlib>=3.5
pyyaml>=6.0
tensorflow>=2.10,<2.16
requests>=2.28

# Optional demo extras (Streamlit + Plotly)
streamlit>=1.30
plotly>=5.18

# Optional speedup
numba>=0.56; platform_system != "Windows"
