# Demo / README images

Drop static screenshots or short GIFs here for the root `README.md` or
`docs/` pages (for example Streamlit sidebar, twin-axis chart, training
reward curve). Filenames are referenced manually from markdown — this
folder is intentionally lightweight and **does not** ship large binaries
in git by default; prefer linking to external hosting for heavy media.
