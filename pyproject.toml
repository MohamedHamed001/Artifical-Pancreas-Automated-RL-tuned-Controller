[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ap-rl"
version = "0.1.0"
description = "Artificial Pancreas RL-tuned PID controller: Hovorka virtual patient + A2C PID tuner + Streamlit digital-twin demo."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Mohamed A. Hamed" }]
# TensorFlow does not yet ship wheels for Python 3.13+ on all platforms; use 3.9–3.12 for full RL training.
requires-python = ">=3.9,<3.13"
keywords = [
    "reinforcement-learning",
    "diabetes",
    "type-1-diabetes",
    "hovorka",
    "pid",
    "a2c",
    "digital-twin",
    "artificial-pancreas",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]

dependencies = [
    "numpy>=1.21",
    "scipy>=1.7",
    "pandas>=1.3",
    "matplotlib>=3.5",
    "pyyaml>=6.0",
    "tensorflow>=2.10",
    "requests>=2.28",
]

[project.optional-dependencies]
demo = [
    "streamlit>=1.30",
    "plotly>=5.18",
]
dev = [
    "pytest>=7",
    "pytest-mock>=3.10",
]
fast = [
    "numba>=0.56",
]

[project.scripts]
ap-rl-download = "ap_rl.scripts.download_checkpoints:main"
ap-rl-train = "ap_rl.training.train_a2c:main"

[project.urls]
Homepage = "https://github.com/your-org/Artifical-Pancreas-Automated-RL-tuned-Controller"
Issues = "https://github.com/your-org/Artifical-Pancreas-Automated-RL-tuned-Controller/issues"

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["ap_rl*"]

[tool.setuptools.package-data]
"ap_rl" = ["py.typed"]

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
testpaths = ["tests"]
